# 🚗 CRM система для автомойки

Полнофункциональная CRM система для управления автомойкой с визуальным расписанием боксов.

## 🎯 Основные возможности

- ✅ **Управление клиентами** - добавление, поиск по телефону, история заказов
- ✅ **Управление автомобилями** - база автомобилей, поиск по гос. номеру
- ✅ **Управление услугами** - настройка услуг, цен и длительности
- ✅ **Управление боксами** - настройка количества и названий боксов
- ✅ **Визуальное расписание** - интерактивный дашборд с временной шкалой
- ✅ **Создание заказов** - умная форма с автозаполнением данных
- ✅ **Отслеживание статусов** - ожидает, в работе, завершен
- ✅ **Анимация прогресса** - визуальное отображение выполнения заказа

## 🏗️ Технологии

### Backend
- **Python 3.x** - основной язык
- **Flask** - веб-фреймворк
- **SQLAlchemy** - ORM для работы с БД
- **PostgreSQL** - база данных
- **Flask-CORS** - поддержка CORS для API

### Frontend
- **React 18** - UI библиотека
- **React Router** - маршрутизация
- **Axios** - HTTP клиент
- **CSS3** - стилизация

## 📁 Структура проекта

```
CRM_car_wash/
├── backend/
│   ├── app.py                 # Основное приложение Flask
│   ├── migrate_db.py          # Скрипт миграции БД
│   ├── test_api.py            # Тесты API
│   ├── start_server.bat       # Запуск сервера (Windows)
│   └── requirements.txt       # Зависимости Python
│
├── frontend/
│   ├── src/
│   │   ├── components/        # React компоненты
│   │   │   ├── Schedule.js    # Главная страница с расписанием
│   │   │   ├── NewOrderModal.js  # Форма создания заказа
│   │   │   ├── Clients.js     # Управление клиентами
│   │   │   ├── Cars.js        # Управление автомобилями
│   │   │   ├── Services.js    # Управление услугами
│   │   │   ├── Orders.js      # Список заказов
│   │   │   └── Settings.js    # Настройки боксов
│   │   ├── App.js             # Главный компонент
│   │   └── index.js           # Точка входа
│   └── package.json           # Зависимости Node.js
│
├── КАК_ЗАПУСТИТЬ.md           # Подробная инструкция запуска
├── MIGRATION_GUIDE.md         # Руководство по миграции БД
└── README.md                  # Этот файл
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

**Backend:**
```bash
cd backend
pip install -r requirements.txt
```

**Frontend:**
```bash
cd frontend
npm install
```

### 2. Настройка базы данных

1. Убедитесь что PostgreSQL установлен и запущен
2. Создайте базу данных `crm_car_wash` через pgAdmin
3. Запустите миграцию:
```bash
cd backend
python migrate_db.py
```

### 3. Запуск приложения

**Backend (в первом терминале):**
```bash
cd backend
python app.py
```
Или просто дважды кликните на `start_server.bat`

**Frontend (во втором терминале):**
```bash
cd frontend
npm start
```

### 4. Откройте в браузере

http://localhost:3000

## 🧪 Тестирование

Проверьте что backend работает корректно:

```bash
cd backend
python test_api.py
```

Или откройте в браузере:
- http://localhost:5000/api/health - проверка здоровья
- http://localhost:5000/api/boxes - список боксов
- http://localhost:5000/api/services - список услуг

## 📖 Подробная документация

- **[КАК_ЗАПУСТИТЬ.md](КАК_ЗАПУСТИТЬ.md)** - Пошаговая инструкция запуска с решением проблем
- **[MIGRATION_GUIDE.md](MIGRATION_GUIDE.md)** - Руководство по миграции базы данных

## 🎨 Интерфейс

### Главная страница (Расписание)
- Визуальная таблица с боксами (строки) и временной шкалой (столбцы)
- Временная шкала: последние 2 часа + 4 часа вперед (интервалы 15 минут)
- Заказы отображаются как цветные карточки на временной шкале
- Красная пульсирующая линия показывает текущее время
- Анимация прогресса для заказов в работе

### Создание заказа
1. **Клиент** - поиск по телефону, автозаполнение или создание нового
2. **Автомобиль** - поиск по гос. номеру, автозаполнение или создание нового
3. **Детали заказа** - выбор услуг (множественный выбор), бокса и времени
4. Автоматический расчет общей стоимости и длительности

### Управление данными
- **Клиенты** - список всех клиентов с телефонами
- **Автомобили** - база автомобилей с гос. номерами
- **Услуги** - настройка услуг, цен и длительности
- **Заказы** - полный список заказов с фильтрацией
- **Настройки** - управление боксами (добавление, редактирование, удаление)

## 🔧 Конфигурация

### База данных

По умолчанию используется:
```
postgresql://postgres:1234@localhost:5432/crm_car_wash
```

Измените в `backend/app.py` если нужны другие параметры:
```python
app.config['SQLALCHEMY_DATABASE_URI'] = 'postgresql://USER:PASSWORD@HOST:PORT/DATABASE'
```

### Порты

- Backend: `http://localhost:5000`
- Frontend: `http://localhost:3000`

### Часовой пояс

По умолчанию: `Europe/Moscow`

Измените в `backend/app.py`:
```python
local_tz = pytz.timezone('Europe/Moscow')  # Ваш часовой пояс
```

## 🐛 Решение проблем

### CORS ошибка в браузере
**Проблема:** `Access to XMLHttpRequest blocked by CORS policy`

**Решение:** Backend сервер не запущен. Запустите `python app.py` или `start_server.bat`

### Боксы не отображаются
**Проблема:** На главной странице пусто

**Решение:** 
1. Проверьте что backend запущен
2. Откройте http://localhost:5000/api/boxes
3. Если пусто, запустите `python migrate_db.py`

### Ошибка подключения к БД
**Проблема:** `Connection refused` или `could not connect to server`

**Решение:**
1. Убедитесь что PostgreSQL запущен
2. Проверьте данные подключения в `app.py`
3. Создайте базу `crm_car_wash` если её нет

## 📝 API Endpoints

### Клиенты
- `GET /api/clients` - список всех клиентов
- `GET /api/clients/search?phone=XXX` - поиск по телефону
- `POST /api/clients` - создать клиента
- `PUT /api/clients/:id` - обновить клиента
- `DELETE /api/clients/:id` - удалить клиента

### Автомобили
- `GET /api/cars` - список всех автомобилей
- `GET /api/cars/search?license_plate=XXX` - поиск по номеру
- `POST /api/cars` - создать автомобиль

### Услуги
- `GET /api/services` - список всех услуг
- `POST /api/services` - создать услугу

### Боксы
- `GET /api/boxes` - список всех боксов
- `POST /api/boxes` - создать бокс
- `PUT /api/boxes/:id` - обновить бокс
- `DELETE /api/boxes/:id` - удалить бокс

### Заказы
- `GET /api/orders` - список всех заказов
- `GET /api/orders/schedule` - заказы для расписания (±6 часов)
- `POST /api/orders` - создать заказ
- `PUT /api/orders/:id` - обновить заказ

### Системные
- `GET /api/health` - проверка здоровья сервера
- `GET /api/settings` - получить настройки
- `POST /api/settings` - обновить настройки

## 📄 Лицензия

Этот проект создан для учебных целей.

## 👨‍💻 Автор

Создано с помощью Kiro AI для автоматизации работы автомойки.

---

**Нужна помощь?** Читайте [КАК_ЗАПУСТИТЬ.md](КАК_ЗАПУСТИТЬ.md) для подробных инструкций!


## ☁️ Деплой на Vercel

Проект настроен для деплоя **всего приложения** (фронтенд + бэкенд) на Vercel.

### Быстрый деплой

1. **Подключите репозиторий к Vercel**
   - Зайдите на https://vercel.com/new
   - Выберите ваш Git репозиторий
   - Root Directory: `CRM_car_wash_API`
   - Нажмите Deploy

2. **Настройте переменные окружения**
   
   В Vercel Dashboard → Settings → Environment Variables:
   ```
   POSTGRES_URL=postgresql://user:pass@host:5432/db
   JWT_SECRET=your-super-secret-key-min-32-chars
   DEFAULT_OWNER_LOGIN=owner
   DEFAULT_OWNER_PASSWORD=secure-password-123
   ```

3. **Инициализируйте БД**
   
   После деплоя откройте: `https://your-app.vercel.app/api/init-db`

4. **Готово!**
   
   Откройте `https://your-app.vercel.app` и войдите с логином владельца.

### Подробная документация

- **[VERCEL_QUICK_START.md](VERCEL_QUICK_START.md)** - Быстрый старт за 5 минут
- **[VERCEL_DEPLOY_GUIDE.md](VERCEL_DEPLOY_GUIDE.md)** - Полное руководство по деплою
- **[VERCEL_CHANGES_SUMMARY.md](VERCEL_CHANGES_SUMMARY.md)** - Резюме всех изменений

### Что было исправлено для Vercel

✅ Исправлены префиксы API роутов (`/api/*`)  
✅ Создана структура для Vercel Serverless Functions  
✅ Настроен фронтенд для работы с API на том же домене  
✅ Добавлен эндпоинт для инициализации БД  
✅ Создана полная документация по деплою  

**Важно**: После инициализации БД удалите эндпоинт `/api/init-db` из `backend/app.py` для безопасности!
